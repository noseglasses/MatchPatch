Add an emoji after the status word displayed in the Progress pane. E.g. something that represents a celebration if "Completed" is displayed and a warning symbol if something goes wrong and also a suitable emoji that represents measurment going on. 

Remove the colum header of the first colum "Selected". It is already obvious that this column is about selection.